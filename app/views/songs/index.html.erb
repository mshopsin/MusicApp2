<ul>
<% @songs.each do |song| %>
 <li>
   <%= song.name %> <%= link_to "Edit", edit_song_path(song) %>,
    <%= link_to "Destroy", song_path(song.id), method: :delete %><br>
 </li>
 <% end %>
</ul>
<%= link_to "<<Add Song>>", new_song_path%>